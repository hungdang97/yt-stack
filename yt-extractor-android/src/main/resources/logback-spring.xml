<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender disabled for production -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger for DownloaderImpl - disabled -->
    <logger name="com.youtube.api.config.DownloaderImpl" level="OFF" additivity="false">
    </logger>

    <!-- Root logger - OFF for production -->
    <root level="OFF">
    </root>
</configuration>
